#include <iostream>
#include <vector>

#include "backend_optimizer.h"
#include "publisher_subscriber.h"
#include "types.h"

int main() {
  std::cerr << "publisheStarts\n";

  BackendOptimizer optimizer;

  // std::bind(&BackendOptimizer::CallbackSubmapList, std::placeholders::_1));

  return 0;
}